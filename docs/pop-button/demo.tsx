import { PopButton } from "@/registry/spell-ui/pop-button";

export function Demo() {
  return (
    <div className="flex items-center justify-center">
      <PopButton className="font-semibold">
        Button
      </PopButton>
    </div>
  );
}